@page
@model ProductPromotion.ConfirmationModel
@{
    ViewData["Title"] = "Confirmation";
}

<div class="container contact-container">
    <h1 class="mt-4 mb-3">
        Onaylandı
    </h1>
    <hr />
    @if (!string.IsNullOrEmpty(Model.Message))
    {
        <h3>@Model.Message</h3>
    }
    <br />
    <p>Başka sorunuz varsa, 500-000-0000 numaralı telefondan bize ulaşabilirsiniz..</p>
</div>